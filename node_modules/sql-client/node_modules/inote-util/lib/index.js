// Generated by CoffeeScript 1.8.0
(function() {
  var HOMEDIR, LIB, LIB_COV, LIB_DIR, exported, file, fs, k, path, sources, v, _i, _len;

  fs = require('fs');

  path = require('path');

  HOMEDIR = path.join(__dirname, '..');

  LIB_COV = path.join(HOMEDIR, 'lib-cov');

  LIB = path.join(HOMEDIR, 'lib');

  LIB_DIR = fs.existsSync(LIB_COV) ? LIB_COV : LIB;

  sources = ['util', 'stopwatch', 'config'];

  for (_i = 0, _len = sources.length; _i < _len; _i++) {
    file = sources[_i];
    exported = require(path.join(LIB_DIR, file));
    for (k in exported) {
      v = exported[k];
      exports[k] = v;
    }
  }

}).call(this);
