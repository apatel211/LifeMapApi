// Generated by CoffeeScript 1.8.0
(function() {
  var HOMEDIR, LIB, LIB_COV, LIB_DIR, fs, index, path, should;

  should = require('should');

  fs = require('fs');

  path = require('path');

  HOMEDIR = path.join(__dirname, '..');

  LIB_COV = path.join(HOMEDIR, 'lib-cov');

  LIB = path.join(HOMEDIR, 'lib');

  LIB_DIR = fs.existsSync(LIB_COV) ? LIB_COV : LIB;

  index = require(path.join(LIB_DIR, 'index'));

  describe("index", function() {
    it("exports Util and Sequencer", function(done) {
      index.Util.should.exist;
      index.Sequencer.should.exist;
      return done();
    });
    it("exports Stopwatch", function(done) {
      index.Stopwatch.should.exist;
      return done();
    });
    return it("exports config", function(done) {
      index.config.should.exist;
      return done();
    });
  });

}).call(this);
